{
  "name": "@bot/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/client.js",
  "scripts": {
    "migrate": "node src/migrate.js",
    "migrate:reset": "node src/migrate.js --reset",
    "test": "node --test tests/*.test.js"
  },
  "dependencies": {
    "pg": "^8.16.0",
    "pgvector": "^0.2.0"
  }
}
